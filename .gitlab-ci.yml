image: maven:3.6-jdk-8

variables:
#  MAVEN_CLI_OPTS: "-s .m2/settings.xml --batch-mode"
  MAVEN_OPTS: "-Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository"

before_script:
# SB: yuck.. surely there must be a better way to install npm:
  - curl -sL https://deb.nodesource.com/setup_8.x | bash - && apt-get install -yq nodejs build-essential

cache:
  paths:
    - .m2/repository
    - backend/target/
    - frontend/node_modules/

build:
  stage: build
  script:
    - mvn $MAVEN_OPTS $MAVEN_CLI_OPTS clean package -Dimplementation-version=${CI_COMMIT_SHORT_SHA}
  artifacts:
    reports:
      junit:
        - backend/target/surefire-reports/TEST-*.xml
        - backend/target/failsafe-reports/TEST-*.xml

karma-test:
  stage: test
  script:
    - cd frontend && npm run test-remote

e2e-test:
  stage: test
  script:
    - cd frontend && npm run e2e-remote
