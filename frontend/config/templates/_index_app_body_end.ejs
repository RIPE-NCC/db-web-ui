<!-- Usersnap widget: always on -->
<script>
    window.onUsersnapLoad = function(api) {
        window.Usersnap = api;
        api.init({
            fields: {
                comment: {
                    defaultValue: '',
                    label: 'Your message',
                    required: true,
                    multiline: true
                },
                email: {
                    defaultValue: RIPE.usermail,
                    label: 'Your E-mail',
                    required: true
                }
            },
            button: {
              icon: null,
              label: 'Report a Bug',
              position: 'bottomRight'
            }
        });

        api.on("open", function(event) {
            event.api.setValue('customData', "db-web-ui-" + angular.element(document).injector().get('Properties').BUILD_TAG);
        });
    }
</script>
<script src="//api.usersnap.com/load/b175ab64-1ecd-40a6-b5e3-ff8a1516fe6f.js?onload=onUsersnapLoad" async ></script>
<!-- Matomo Tag Manager -->
<script type="text/javascript">
    var matomoContainerId = angular.element(document).injector().get('Properties').MATOMO_ID;
    var _mtm = _mtm || [];
    _mtm.push({'mtm.startTime': (new Date().getTime()), 'event': 'mtm.Start'});
    var d=document, g=d.createElement('script'), s=d.getElementsByTagName('script')[0];
    g.type='text/javascript';
    g.async=true; g.defer=true;
    g.src="https://www-analytics.ripe.net/js/container_" + matomoContainerId + ".js";
    s.parentNode.insertBefore(g,s);
</script>
<!-- End Matomo Tag Manager -->
